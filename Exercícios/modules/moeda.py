def aumentar(arg, aug):
    """Function destinates to calc the augment of ammount the value input in it.

    Args:
        arg (float): the value that wants to calculate.
        aug (int): ammount that needs to be augment on value.
    """
    tot = arg + aug
    
    return tot


def diminuir(arg, sub):
    """function destiantes to subtract the value.

    Args:
        arg (float): the value that wants to calculate.
        sub (int): the value that needs to be subtract on value.
    """
    
    tot = arg - sub
    
    return tot

def metade(arg):
    """function destinates to calc the half of value.

    Args:
        arg (float): the value that wants to calculate.
    """
    tot = arg / 2
    
    return tot