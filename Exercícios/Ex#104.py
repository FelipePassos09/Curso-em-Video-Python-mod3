'''Crie um programa que tenha a função leiaInt() que vai funcionar de forma semelhante ao input do python, so que fazendo a validação para aceitar apenas um valor numerico.'''

def leiaInt(n=int):
    """This function returns an error when a number inputed in your argument is not
    a int 

    Args:
        n ([type], optional): [description]. Defaults to int.
    """
    
    